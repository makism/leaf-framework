
<IfModule mod_rewrite.c>
    RewriteEngine	On
    RewriteBase		/leaf/
    RewriteCond		$1	!^(index\.php|.+\.(css|js|ico|jpeg|jpg|png|gif)|applications/common)
    RewriteRule		^(.*)$	index.php/$1 [L]
</IfModule>

